# Convex Configuration
# After running `npx convex dev`, this URL is automatically written to .env.local
# You can also find it in: https://dashboard.convex.dev → Your Project → Settings

VITE_CONVEX_URL=https://your-deployment-name.convex.cloud
